.DS_Store

# .github/ stond hier. Dat betekende dat elk workflowbestand onzichtbaar was voor git: de map bestond
# lokaal, maar wat je aanpaste kwam nooit op GitHub aan en de wijziging deed dus niets. Weggehaald.

test/uitvoer/
test/node_modules/
server/node_modules/
# v23.178: de repo-root heeft sinds de lintstap ook een package.json. Alleen eslint, alleen
# no-undef, alleen op tools/.
/node_modules/
/package-lock.json

_to_delete/
.claude/

# De patchbestanden waarmee ik werk uitlever horen niet in de repo. Ze slopen sinds v23.159 elke
# aflevering: git add -A pakte ze mee, en dan probeert de patch bij Stefan een bestand aan te maken
# dat bij hem al los in de map ligt ("already exists in working directory"). git am weigert dan, en
# laat een halve .git/rebase-apply achter waar elke volgende poging over struikelt.
vamos-*.patch
